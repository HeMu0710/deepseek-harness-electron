/**
 * @deepseek-ai/dsh-electron-app — Electron surface rows over dsh-gui-app. The
 * package's substance is `cordis.patch.yml`, resolved through
 * `dsh.bundle.patch`; this module has no runtime API.
 * @module @deepseek-ai/dsh-electron-app
 */

export {}
