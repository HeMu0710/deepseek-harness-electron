/**
 * @deepseek-ai/dsh-gui-app — shared GUI profile rows. The package's substance
 * is `cordis.patch.yml`, resolved through `dsh.bundle.patch`; this module has
 * no runtime API.
 * @module @deepseek-ai/dsh-gui-app
 */

export {}
